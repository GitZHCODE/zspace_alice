#include "Vector.h"

namespace alice2 {

    // Vector implementation is mostly header-only for performance
    // This file exists for potential future implementations that require
    // separate compilation (e.g., complex vector operations, SIMD optimizations)

} // namespace alice2
